tp = 100, 200, "함수", 100, "개수"
# index()
print(f"tp 안의 200의 위치 : {tp.index(200)}")
# 찾는 값이 없으면 index 에러
# print(f"tp 안의 200의 위치 : {tp.index(20)}")
# count()
print(f"tp 안의 100의 갯수 : {tp.count(100)}")
print(f"tp 안의 100의 갯수 : {tp.count(10)}")